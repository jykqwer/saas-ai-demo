apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: saas-ai
spec:
  selector:
    app: saas-ai
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
