apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: saas-ai
spec:
  type: ClusterIP
  selector:
    app: saas-ai
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
